# vieww_artifacts

Artifacts for the vieww repository.

- `film_lab/` — the element experiments: **78 rendered plates** (rounds 2–10) with source, a loopable `anim.gif` motion receipt per plate, contact sheets, metrics receipts, the pixel-probe evidence plate, and the premium-UI reference run. The per-plate reviewable set is the **sheet + GIF + metrics** — individual `frame_*.png` files are intentionally not stored (1,494 frames / 1.4 GB removed at the round-10 close-out; every frame is byte-reproducible from the deterministic source). Round 6 was the berserk spectrum — the shape-count record (`galaxy`, 60,527 shapes/frame in 101 ms), the rect ceiling (`mandel`, 57,602/frame in 69 ms), the 4K master receipt (`hero4k`), glyph-run storms, procedural recursion, a hashed night city, a lensed black hole, a 4D tesseract, and the three genesis arcs (line → 3D avatar, text → blurred fade-away, drop → entire ocean). Round 7 was the deep axes — the script axis (`han`, CJK through the shaper at 107 glyph runs/frame), the single-path axis (`megapath`, one 40k-segment line), the endurance axis (`longplay`, 256 frames with a flat kernel-RSS series), the simulation axis (`swarm`, 2,400 boids with the build/raster split receipted), the blend-mode matrix (`blendmatrix`, 16/16 modes distinct, non-separable faithful), compositor depth (`filterstack`, nested filters + σ sweep), the blur economy at 30-of-32 on the U-06 guard (`shadowplay`), and animated 256-stop gradients (`prism`) — which found U-23, the silent 8-stop truncation, on its first day. Round 8 was the wonder engines — the machines the lab only imagined: a total eclipse as a documented light sequence (`eclipse`), 7,000 Chladni grains descending to nodal lines (`cymatics`), a 32-pendulum phase choir that rephases exactly (`harmony`), the avatar line re-drawn by its own Fourier choir (`fourier`), a long-exposure star field whose Plus-blended trails accumulate like silver (`startrail`), soap-film iridescence from a 48-wavelength interference integral (`bubble`), a clockwork orrery whose gear ratios ARE its astronomy with a Kepler-integrated comet (`orrery`), a supercell with recursive lightning (`storm`), a D-12 kaleidoscope whose mirror residual is measured from the raster (`kaleido`), and one drop of ink that becomes a nebula, disintegrates and dilutes (`ink`). **Round 9 is the law machines — twelve plates where the law of nature is the machine, each receipt closing the law's own books: the double-slit with its Born-rule fringes measured from the raster against λL/d (`quantum`), a staggered-MAC Navier–Stokes solver whose vortex street sheds at a measured Strouhal (`smoke`), the figure-eight three-body choreography run twice from δ = 10⁻⁵ with the divergence fitted (`threebody`), Gray–Scott morphogenesis on a five-regime tour (`turing`), Galton's bean machine at σ measured −0.5% vs theory (`galton`), pool-floor caustics where the envelope condition and the ray histogram agree (`caustics`), an Ising magnet whose order arrives at T ≈ 2.2, beside Onsager's 2.269 (`ising`), a quasicrystal whose forbidden C10 symmetry is measured from its own pixels (`crystal`), a neural network that holds its plateau then clicks to 100% (`neural`), a Pratt truss solved by method-of-joints live with ΣFy closed at 10⁻⁵ (`truss`), Rule 30 / Rule 110 / Life as spacetime pictures with the centre column's balance measured (`cellauto`), and a collider event display where r = pT/qB is the arc and the MET arrow closes the ledger (`collider`).** **Round 10 is the closing round — the machines of emergence and signature: the BTW sandpile (τ = 1.174 beside the 2D exponent 1.25), percolation's threshold bracketed by bisection, Langton's ant highway (period 104, displacement (2,2), counted from its own trace), a DLA aggregate at D = 1.71, the Lorenz butterfly (λ = 0.878 by Benettin beside 0.906), the double pendulum holding energy to 1.1×10⁻⁸ above its 336-seed revolution basin, a de Bruijn pentagrid breathing through φ² with its forbidden C5 measured from the raster, the dragon and Hilbert curves with box-counting D = 1.764 read from the output buffer, the gravitational-wave chirp at +0.6% of the law's 11/3, Foucault's −271.06°/day closed from its own 24 petal axes, a kernel-painted double rainbow with both bows measured from the final frame's luminance profile, and six sorts racing on one seeded permutation with every comparison and write counted from the traces.**
- `vieww_base/` — the vieww framework source itself (the full Rust workspace: 36 crates, examples, docs, CI). **All updates to vieww land here.** The `todo-upgrades.md` items are fixed in this tree — each entry's status and the change that resolved it are annotated in place. Round 5 landed `guard_filtered_layers_below` (U-06), `SceneReport::open_subpath_fills` + `Path::open_subpaths` (U-20's counter), and the full-turn `Path::arc` closing (U-22 — the census's first catch).
- `todo-upgrades.md` — the bench's upgrade notes, annotated with resolutions. U-01 through U-22; the remaining OPEN item (U-18, the hairline policy) now carries the probe plate's measured rung-ink ladder.
